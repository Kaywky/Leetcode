class Solution {
public:
    string reverseString(string s) {
        int len = s.length();
        int i;
        int temp;
        for(i=0; i<len/2; i++ )
        {
            temp = s[len-1-i];
            s[len-1-i] = s[i];
            s[i] = temp;
        }
        return s;
    }
};
